import React from "react";
import { render } from "@testing-library/react";
import { Provider } from "react-redux";
import { store } from "../state/index";
import App from "../App";

test("renders learn react link", async () => {
  const pet = render(<App />);

  expect(input).toBe(0);
});
